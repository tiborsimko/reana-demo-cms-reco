version: 0.3.0
workflow:
  type: serial
  specification:
    steps:
      - environment: 'python:3.7-slim'
        commands:
          - ls -l /cvmfs > z1.txt
          - find /cvmfs/cms-opendata-conddb.cern.ch > z2.txt

